# Copy to terraform.tfvars (gitignored). Prefer OpenBao export in CI — see scripts/export-openbao-for-tofu.sh

coolify_endpoint = "https://coolify.bdziam.dev"
# coolify_api_token — from secret/beskid/tofu/production

openbao_address = "https://openbao.example.com"
# openbao_token — AppRole or admin token

project_uuid = "tosg8kc80g8go00sgcswsccg"
server_uuid  = "ec0cs0cw0ocsok488gc0k80k"

manage_environment = false

# Pilot: enable one service at a time, then expand.
# enable_services = { site = true, auth = true, tracker = false, nexus = false, pckg = false }
