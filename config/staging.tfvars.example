# Copy to config/staging.tfvars (gitignored)

coolify_endpoint = "https://coolify.bdziam.dev"
openbao_address  = "https://secrets.bdziam.dev"

# UUID from production output or: scripts/ci/resolve-coolify-project-uuid.sh
# project_uuid = "..."

server_uuid = "ec0cs0cw0ocsok488gc0k80k"

manage_environment = true

enable_services = {
  site    = true
  auth    = true
  tracker = false
  nexus   = false
  pckg    = false
}
